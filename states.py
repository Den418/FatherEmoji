# Простейшее FSM: user_id -> словарик, где "state" — текущая стадия,
# остальное — данные (ссылка пака, название, режим векторизации и т.п.).
user_states: dict[int, dict] = {}

# Последний выбранный пользователем режим векторизации ("off"/"on"/"ask").
# Живёт дольше одной сессии, чтобы при следующем паке предлагать его же.
vector_prefs: dict[int, str] = {}
